---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: upfclasses.nf.nephio.org
spec:
  group: nf.nephio.org
  names:
    kind: UpfClass
    listKind: UpfClassList
    plural: upfclasses
    singular: upfclass
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UpfClass is the Schema for the upfclasses API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: UpfClassSpec defines the desired state of UpfClass
            properties:
              dnn:
                items:
                  type: string
                type: array
              n3endpoints:
                type: integer
              n4endpoints:
                type: integer
              n6endpoints:
                type: integer
              n9endpoints:
                type: integer
              packageRef:
                description: PackageRevisionReference is used to reference a particular
                  package revision.
                properties:
                  namespace:
                    description: Namespace is the namespace for both the repository
                      and package revision
                    type: string
                  packageName:
                    description: PackageName is the name of the package for the revision
                    type: string
                  repository:
                    description: Repository is the name of the repository containing
                      the package
                    type: string
                  revision:
                    description: Revision is the specific version number of the revision
                      of the package
                    type: string
                required:
                - packageName
                - repository
                - revision
                type: object
            required:
            - n3endpoints
            - n4endpoints
            - n6endpoints
            - n9endpoints
            - packageRef
            type: object
          status:
            description: 'UpfClassStatus defines the observed state of UpfClass TODO:
              we need a controller to validate that the packageRef is valid and that
              the underlying package is Ready'
            properties:
              ready:
                description: Specifies whether the UpfClass is ready to be used
                type: boolean
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
